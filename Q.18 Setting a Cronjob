Q.18 Setting a Cronjob 
• Create a cronjob for user natasha in all nodes, the playbook name crontab.yml and the 
job details are that it in every 2 minutes the job will execute logger "EX294 in progress

solution
$ vim crontab.yml 
$ ansible-playbook crontab.yml 
• To Verify: 
• ansible all -m command -a “crontab -l"


